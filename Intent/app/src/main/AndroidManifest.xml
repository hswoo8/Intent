<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hswoo.intent">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SingleTopActivity"
            android:launchMode="singleTop" />
        <activity
            android:name=".SingleInstanceActivity"
            android:launchMode="singleInstance" />
        <activity
            android:name=".SingleTaskActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".StandardActivity"
            android:launchMode="standard" />
        <activity android:name=".BaseActivity" />
        <activity android:name=".flag.NewTaskActivity" />
        <activity android:name=".flag.SingleTopActivity" />
        <activity android:name=".flag.ClearTopActivity" />
        <activity  android:name=".flag.BaseActivity" />
        <activity android:name=".flag.NoFlagActivity"></activity>
    </application>

</manifest>